## Craftsman Position — `--page` flag to reopen the live session page

**Core recommendation:** Ship `--page [<slug>]` as a session-management execute-and-exit flag that reuses two existing primitives verbatim — the `--resume` slug resolver and the contract item-3 "rerun scribe + open" command — adding zero executable code and zero net test surface. The flag must be defined only in `_council-engine.md`, but discoverability duplication in `council.md` forces two extra edits I want tracked, not skipped.

**Key argument:**
The engine already contains every mechanic `--page` needs; the win is composition, not invention. Placement: a new item in the priority list (`commands/_council-engine.md:52-58`) alongside `--list`/`--status`/`--cleanup`, marked "execute and exit — no session started." Slug resolution must **call the existing Resume Logic** (`_council-engine.md:1615-1631`): no slug → most recent `active` session; `<slug>` → fuzzy prefix match on the index; ambiguous → `AskUserQuestion` picker. Reusing that resolver (rather than writing a second "find the session" path) is my Rule-of-Three non-negotiable — three parallel resolvers is exactly the drift this repo's governance spec fights. The reopen action is `_council-engine.md:253-254` verbatim: rerun `render-session.py` (cheap, idempotent — proven by `test_regeneration_is_idempotent` in `pipeline/tests/test_render_session.py:90`), then `open || xdg-open || echo`. Rerunning the scribe first is free freshness and safe on finished sessions (`test_complete_session_drops_refresh_tag:77` proves a `complete:true` reopen does **not** re-add the refresh tag). Because the flag adds no runtime code, the honest testing answer is: the existing `test_render_session.py` suite already covers the only executable surface it touches. What genuinely needs review is **consistency**, not coverage — the flag string must appear identically in four places: the priority list, the `--help` SESSION MANAGEMENT block (`_council-engine.md:160-169`), a new `### --page` subsection under Session Management Commands, and — the trap — `council.md`'s `argument-hint` frontmatter (`council.md:3`) and examples block (`council.md:22-30`), which redundantly enumerate flags per theme. "Inherited for free via the engine" is true for parsing but false for discoverability; every themed variant (finance-council, etc.) and the sibling `agentic-council` engine carries its own copy of that hint string, so this is a five-surface edit, not one.

**Edge cases that need explicit handling (pit-of-success, not silent surprise):**
- **Session predates the live page (pre-F11):** no `session.html` exists. `--page` must NOT start work or a session — print `This session predates the live page; run --resume to regenerate it` and EXIT. Mirror the intent of resume item 6 but without the resume side effects.
- **brainstorm-mode session:** never has a page. Message clearly and exit.
- **No active sessions at all:** friendly "no active session to open" message, not a stack trace or empty picker.

**Risks if ignored:**
- **Pattern drift (regression risk):** a hand-rolled slug resolver inside `--page` diverges from `--resume` the first time resume's fuzzy-match logic changes; two behaviors for "which session did you mean" is a maintenance tax and a user-confusion bug.
- **Consistency rot (DX risk):** if `council.md`'s `argument-hint` and examples aren't updated in lockstep, the flag works but is undiscoverable, and the help text lies. This repo's pre-commit validators check frontmatter/reference integrity; a partial edit either fails the hook or, worse, passes while docs disagree.
- **Silent-failure DX:** the `open ... || echo` fallback already degrades gracefully, but the pre-F11 / brainstorm / no-session cases have no existing handler — without explicit branches the conductor will improvise inconsistently per run.

**Dependencies on other domains:**
- **Architect:** confirm `--page` belongs in the session-management (execute-and-exit) tier and does not need a session lock or index write — my read is it's pure read-only reopen; please validate against the index/registry ownership model.
- **Advocate:** decide the exact copy for the three "nothing to open" messages so the UX is warm and points to the right next flag (`--resume` vs `--list`).
- **Whoever owns cross-repo parity (`agentic-council`):** the same five-surface patch must land there; I want this called out as a deliberate follow-up, not assumed automatic.
