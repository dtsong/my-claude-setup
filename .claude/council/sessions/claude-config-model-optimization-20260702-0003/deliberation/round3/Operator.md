# Operator Final Position — Claude Config & Model-Routing Optimization

**Revised recommendation:** Ship the v1 quick-win PR as four ungated config fixes plus one split-out ops fix: F1 model-pin alias, F2 permissions, F4 stale-ID refresh, F5 unified routing DOC, and **F3a** (fail-soft existence guard on the telemetry hook + private-path removal from public `settings.json`). Defer F3b (vendoring, per-call interpreter batching, alert tuning) to v1.1. Any *live model-behavior flip* (first `routed_consult` caller, `/ship` or council-profile changes) carries a 12-case smoke eval as an acceptance criterion. Prune skills on evidence only, and the evidence window does not start counting until the telemetry increment is made durable.

## Concessions made and why

- **I retract "telemetry now works."** My Round 1 claim rested on seeing `uses:1` in `registry.json`. Craftsman and Scout verified this was my own session's uncommitted write: committed HEAD shows 0/67 uses across 4+ prior council sessions. `skill-telemetry.sh` is unwired and at the wrong layer (council skills load inline via `skillContent`, engine line 612); the real increment is engine-level (line 672-673) and only fired because we loaded skills today. So instrument-then-prune survives, but with a hard precondition: the increment must be made **durable** (untracked sidecar file or auto-commit, per Craftsman) before the 2-4 week window is declared started. Zero-uses stays inconclusive; any-use is keep.
- **I conceded F3b to v1.1 and accept I overstated blast radius on this Max account.** Strategist is right that on the symlinked local account the bare hook path resolves fine today. The XS piece (existence guard + vendored wrapper) is what ships v1; the M-sized hardening (interpreter perf, batching, alert thresholds) waits. Strategist re-scored F3a to RICE 1350 and pulled it into the MVP alongside F1/F2/F4/F5, which resolves the cut-line inconsistency I flagged.
- **I defer routing *activation* to Oracle/Strategist's eval-before-activation rule.** F5 ships as a DOC in v1 because it flips no live model behavior. I do not need a full 38-case harness to gate a document. The 12-case smoke eval gates the first live flip; the 38-case harness gates Phase 2 relay and prune evidence.
- **I align OpenRouter gating with Skeptic/Scout.** Pattern B stays Hold on Max, Trial on API only after ID refresh and cost ceiling; Pattern A (lens relay) is a gated Trial behind a send-allowlist, ZDR/no-training flags, actual-model audit, and a kill switch. The MCP server stays gated behind a flag until a caller exists.

## Non-negotiables

1. **F3a ships in v1 and is the ship-blocker.** The telemetry hook fires on five lifecycle events (`settings.json` lines 71-115), bare-calling `python3 $HOME/Development/my-claude-setup-private/hooks/session_telemetry.py` with no guard. Fix = fail-soft dispatcher wrapper (five string edits + one ~5-line script) that (a) no-ops silently if the private path is absent, (b) removes the private path from public config, (c) does not break on a fresh clone or the API work account. This is the operational blast-radius fix; it merges even if everything else slips.
2. **Model pin resolved before merge.** `fable-5[1m]` → tier alias; never ship both the `[1m]` suffix and `CLAUDE_CODE_DISABLE_1M_CONTEXT=1`. Pick one path deliberately.
3. **No live model-behavior flip without the 12-case smoke eval as an acceptance criterion** in `pipeline/evals/`. Applies to the first `routed_consult` caller and any `/ship` or council-profile tier change.
4. **Durable telemetry increment lands before the prune-evidence window opens.** No pruning decision cites usage counts collected before the increment is hardened.
5. **OpenRouter MCP server stays gated behind a flag** until a caller is wired; any egress trial requires send-allowlist + no-training/ZDR flags + actual-vendor-model audit logging + kill switch.

## Implementation notes

- **F3a dispatcher (v1):** create `hooks/telemetry-dispatch.sh` in THIS repo. Body: resolve the private hook path from an env var (default to the known location), `[ -f "$path" ] || exit 0`, else `exec python3 "$path" "$@"`. Point all five hook events at the in-repo dispatcher instead of the private absolute path. Net effect: public `settings.json` no longer leaks the private path, fresh clones no-op cleanly, and the API work account stops erroring on every tool call.
- **F3b (v1.1):** vendor or symlink the telemetry package into this repo; batch or debounce the `PostToolUse` interpreter cold-start (~50-150ms per tool call today); tune `cost_sink_alert.py` / `context_decay_alert.py` thresholds once real per-session tier data exists.
- **Durable increment (precondition for prune evidence):** write counts to an untracked `skills/council/registry.uses.jsonl` sidecar (append-only, one line per load) or auto-commit the registry increment; register the increment for standalone suites too, not just council. Reconcile into `registry.json` on a monthly review.
- **F5 routing DOC (v1):** one profile-aware table spanning session default, council profiles (lean/balanced/max), workflow/subagent spawns, `/ship` + `/looper`, and OpenRouter Pattern A/B. Max-plan optimizes quality within rate budget (Fable/Opus default, `balanced` council); API optimizes cost/session (Sonnet default, `lean` council, native Haiku for cheap tasks over a second OpenRouter dependency). Include a model-ID review cadence so IDs never rot again.
- **12-case smoke eval (v1.1 gate):** land in `pipeline/evals/` as the acceptance criterion for the first live flip; expand to the 38-case harness before Phase 2 relay and any prune that cites usage evidence.
- **Extract dormant suites now (independent of telemetry):** ece-* agents, `/ece`, ece skills, resume-tailor, docx-to-pdf, soc-security → private repo. Reversible, record-preserving, shrinks classification/load surface. Not gated on eval or telemetry.
- **Verify every config change** with `pre-commit run --all-files` (governance token budgets + frontmatter + cross-skill isolation) before merge.
